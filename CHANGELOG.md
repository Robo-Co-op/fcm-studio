# Changelog

## 0.1.5

- デモモードにロール切替(owner/editor/viewer)、招待リンク発行UI(演出のみ、実送信・実DBなし)、2エディタ同時編集デモ、Euria AI提案のルールベースなモック(人間の承認待ちUIを含む)を追加した。
- いずれも `src/demo-cloud/` 内で完結し、本番のクラウド実装(`src/cloud/`、`supabase/`、`server/`)には変更を加えていない。

## 0.1.4

- ステークホルダー向けデモ用の完全モック「デモモード」(`VITE_DEMO_MODE=true`)を追加。実Supabase/Google OAuth/Infomaniak連携なしに、疑似ログイン・ダミープロジェクト一覧・実際のマップ/行列/シミュレーション編集・役割別の読み取り専用表示を体験できる。
- デモモードは `src/demo-cloud/` に完全分離して実装し、本番のクラウド実装(`src/cloud/`、`supabase/`、`server/`)には一切変更を加えていない。
- 画面上部に常時デモバナーを表示し、モックデータ・モックAIであることを明示する。

## 0.1.3

- プロジェクト所有者がメールアドレスに紐づく編集者・閲覧者の招待リンクを発行、取消、役割変更、メンバー削除できるようにした。
- 招待トークンはハッシュ化して保存し、7日で失効、一度だけ受諾可能にした。リンクはURLフラグメントで扱い、メール送信は行わない。
- 共有ベースライン、シナリオ、シミュレーション実行履歴を不変・プロジェクト単位・帰属付きで保存し、共同編集画面へ同期した。
- 閲覧者はローカルでシミュレーションできるが、共有モデル・履歴・メンバーシップは変更できないようにした。

## 0.1.2

- オーナー・編集者のマップ、行列、ノード位置、プロジェクト詳細を原子的なrevisionコマンドで共有。
- 競合を明示し、未保存ドラフトの書き出し・破棄・確認後の再適用を追加。
- 同じoperation IDの再試行を二重適用せず、閲覧者・部外者・権限取消後の更新を拒否。
- Realtime更新、focus・再接続・10秒間隔の再取得で他セッションと同期。
- オフライン時は即座に読み取り専用とし、送信中のドラフトを保持。

## 0.1.1

- Google PKCEログイン、非公開プロジェクト一覧、作成、ローカル研究の明示的コピーを追加。
- PostgreSQLの行レベル権限と原子的作成により、プロジェクト単位でデータを分離。
- クラウドの初期画面は読み取り専用。ローカル編集・IndexedDB保存を維持。
- ブラウザとDBの文字・日時検証を統一し、保存量に上限を設定。
- 実PostgreSQLテスト、認証境界を模擬したブラウザテスト、故障注入済み品質ゲートを追加。

## 0.1.0

- マップと重み行列、Excel入出力、シミュレーション、レビュー付きAI提案のローカル版を公開。
